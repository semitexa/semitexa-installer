# Environment — contains secrets, never commit
.env

# Docker Compose local overrides
docker-compose.override.yml

# Composer
vendor/
composer.lock

# Semitexa runtime
var/
*.cache

# OS
.DS_Store
Thumbs.db
